/// This class defines the application's routes for navigation.
///
/// Each route is represented as a static string, which can be used
/// throughout the Flutter app to navigate between different screens.
///
/// Example usage:
/// Navigator.pushNamed(context, AppRoutes.splash);
///
class AppRoutes {
  static String splash = '/';
  static String walkthrough = '/walkthrough';
}